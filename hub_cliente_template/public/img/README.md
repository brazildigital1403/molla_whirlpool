# Logos do projeto

Coloque aqui os arquivos:

- `logo_metlife.svg` — usado no topo de todas as páginas
- `logo_molla.svg` — usado no rodapé de todas as páginas

> Mantenha esses nomes exatos. Se forem alterados, atualize as referências
> nos HTMLs (`<img src="/img/logo_metlife.svg">` e `<img src="/img/logo_molla.svg">`).
